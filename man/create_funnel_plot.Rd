% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myfunnelplot.R
\name{create_funnel_plot}
\alias{create_funnel_plot}
\title{Create a Funnel Plot}
\usage{
create_funnel_plot(
  data,
  estimate,
  se,
  y_column = "y",
  v_column = "v",
  x_label = "Standard Error",
  y_label = "Standardized Mean Difference"
)
}
\arguments{
\item{data}{A data frame containing study data, including an effect estimate
column and a standard error column.}

\item{estimate}{The name of the column containing effect estimates.}

\item{se}{The name of the column containing standard errors.}

\item{y_column}{The name of the column containing the y-axis variable
(effect estimate). Defaults to "y".}

\item{v_column}{The name of the column containing the precision measure
(often the variance). Defaults to "v".}

\item{x_label}{The label for the x-axis. Defaults to "Standard Error".}

\item{y_label}{The label for the y-axis. Defaults to "Standardized Mean Difference".}
}
\value{
A ggplot2 object representing the funnel plot.
}
\description{
This function creates a funnel plot to visualize potential
publication bias or heterogeneity in a meta-analysis. The plot displays
effect estimates against a measure of precision (usually standard error).
Credit to jksakaluk for the logic.
}
\examples{
# Example using sample data
sample_data <- data.frame(
  study = c("Study A", "Study B", "Study C"),
  y = c(0.5, -0.2, 0.1),
  v = c(0.05, 0.08, 0.03)
)
my_funnel_plot <- create_funnel_plot(sample_data, estimate = "y", se = "v")
my_funnel_plot

}
